#include <stdio.h>

//int x[9];


void foo(int a)
{
    int y;
    int x = 2;
    int z[x];
    // y + a = 0;

}

int main() {
    
    float a, b, c;
    a + b;
    a+(b=c);
    // foo(a, b);
    while(a);
}

